#include "pch.h"
#include "../J3RRY_ACCOUNT_BOOK/src/model.h"

TEST(TestCaseName, TestName) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}

TEST(MODEL_TEST,INSERT_AND_READ) {
	
}

int main(int argc, char *argv[])
{
	testing::InitGoogleTest(&argc, argv);
	RUN_ALL_TESTS();
	return 0;
}